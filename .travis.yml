language: python
python:
  - "3.7"
  - "3.7-dev"

# command to install dependencies
before_install:
  - sudo apt get install ffmpeg
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest --cov src/ test/ --verbose